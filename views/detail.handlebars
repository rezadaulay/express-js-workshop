<h4 class="text-center mb-3">Detail Todo</h4>
<div>
    <div class="mb-3">
        <label for="name" class="form-label">Judul</label>
        <p>{{ todo.title }}</p>
    </div>
    <div class="mb-3">
        <label for="description" class="form-label">Deskripsi</label>
        <p>{{ todo.description }}</p>
    </div>
    <div class="mt-3 d-flex">
        <a class="btn btn-primary" href="/edit/{{todo.id}}">Edit</a>&nbsp;&nbsp;&nbsp;&nbsp;<a class="btn btn-danger">Hapus</a>
    </div>
</div>